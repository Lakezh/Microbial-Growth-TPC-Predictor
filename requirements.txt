# Core numerical stack
numpy>=1.24
pandas>=2.0
scikit-learn>=1.4
scipy>=1.11

# Deep learning (TPC PINN)
torch>=2.1

# ODE solver (optional, falls back to RK4 if absent)
torchdiffeq>=0.2.3

# FBA anchor (optional)
cobra>=0.29

# Visualisation (examples only)
matplotlib>=3.8
